import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    padding: 5,
    borderRadius: 20,
    borderWidth: 0.8,
    borderColor: 'grey',
  },
  txt: {
    fontWeight: 'bold',
  },
});

export default styles;
