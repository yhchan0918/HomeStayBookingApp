import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    padding: 20,
    backgroundColor: '#FFF',
  },
  input: {
    fontSize: 20,
    marginBottom: 20,
  },
});

export default styles;
